spring:
  application:
    name: my-spring-app

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  # Database Configuration
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/luanvan?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: baong
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA/Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

# environment variables
app:
  vars:
    cluster_ip: 172.16.123.200
    cluster_port: 22
    cluster_username: devops
    cluster_password: devops
    docker_image_ip: 172.16.123.100
    docker_image_port: 22
    docker_image_username: devops
    docker_image_password: devops